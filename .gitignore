# Build trees
out/
build/

# Generated by script/build/generate-msvc-build-script.sh. Holds absolute paths
# that are different on every machine, so it is regenerated rather than shared.
script/build.sh

compile_commands.json

*.pdb
